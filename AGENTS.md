# CS-Server — Agents

The canonical agent guide for this repository is **`CLAUDE.md`**. Read it first
and follow it for architecture, testing, the BDD lifecycle, observability, and
security rules.

Lifecycle skills (canonical workflows under `.claude/skills/`; Codex metadata
under `.agents/skills/`):

- `plan-feature` — canonical: `.claude/skills/plan-feature/SKILL.md`.
- `execute-feature` — canonical: `.claude/skills/execute-feature/SKILL.md`.

Setup, endpoints, configuration, and deployment live in `README.md` and
`DEPLOYMENT.md`; pytest/Redis/BDD command details live in `tests/README.md`.
